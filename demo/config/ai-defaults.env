# Fallback defaults for deploy scripts — prefer config/ai.env (copy from ai.env.example).
# Checked by scripts/check-conflicts.mjs
LLM_PROVIDER=huggingface
HF_MODEL=meta-llama/Llama-3.1-8B-Instruct
HF_API_BASE=https://router.huggingface.co/v1
LLM_REQUEST_TIMEOUT_MS=600000
HF_ASR_MODEL=openai/whisper-large-v3
ASR_REQUEST_TIMEOUT_MS=600000
